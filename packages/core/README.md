# @costscope/core

Core routing, scope planning, orchestration planning, prompt generation, diff guard, provider configuration, and cost estimation logic for CostScope.

This package is intentionally platform-independent. Local executors, CLIs, MCP servers, GitHub Actions, and future hosted services should remain thin adapters over this core.
